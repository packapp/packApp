// ACTION TYPES

// INITIAL STATE
const initialState = {
  trips: []
};

// ACTION CREATORS

// THUNK CREATORS

// REDUCER
export default (state= initialState, action) => {
  switch(action.type) {
    default:
      return state;
  }
};
